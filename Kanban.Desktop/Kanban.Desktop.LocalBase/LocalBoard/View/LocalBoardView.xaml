<UserControl x:Class="Kanban.Desktop.LocalBase.LocalBoard.View.LocalBoardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:dialogs="clr-namespace:MahApps.Metro.Controls.Dialogs;assembly=MahApps.Metro"
             dialogs:DialogParticipation.Register="{Binding}"
             xmlns:local="clr-namespace:Kanban.Desktop.LocalBase.LocalBoard.View"
             xmlns:dd="clr-namespace:GongSolutions.Wpf.DragDrop;assembly=GongSolutions.Wpf.DragDrop"
             xmlns:kanbanControl="clr-namespace:Ui.Wpf.KanbanControl;assembly=Ui.Wpf.KanbanControl"
             mc:Ignorable="d">
    <Grid>
        <kanbanControl:Kanban HorizontalDimension="{Binding HorizontalDimension}" 
                              VerticalDimension="{Binding VerticalDimension}"
                              Cards="{Binding Issues}"
                              CardContent="{Binding CardContent}"/>
    </Grid>
</UserControl>
