<UserControl x:Class="Kanban.Desktop.LocalBase.Views.Wizard.WizardStep1View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Kanban.Desktop.LocalBase.Views.Wizard"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary Source="WizardTemplatesDictionary.xaml"/>
    </UserControl.Resources>
    <StackPanel Orientation="Vertical">
        <Label>Board name</Label>
        <TextBox Text="{Binding BoardName,
            UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"/>

        <Label>Folder name</Label>
        <TextBox Text="{Binding FolderName,
            UpdateSourceTrigger=PropertyChanged,ValidatesOnDataErrors=True}"/>
        <Button Command="{Binding SelectFolderCommand}">...</Button>

        <Label>File name</Label>
        <TextBox Text="{Binding FileName, 
           UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"/>
    </StackPanel>
</UserControl>
